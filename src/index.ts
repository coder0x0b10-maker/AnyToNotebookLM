/**
 * AnyToNotebookLM - Implementation Logic
 * Bridges AgentReach (DeepReader) with NotebookLM
 */
export { AnyToNotebookLM, Any2NLMOptions } from './any2nlm';

// Legacy export for backward compatibility
export { runBridge } from './legacy';
